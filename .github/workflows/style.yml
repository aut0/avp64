name: style

on:
  push:
    branches: [ master, dev** ]
  pull_request:
    branches: [ master ]

jobs:
  style:
    runs-on: ubuntu-26.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: 'recursive'

      - name: Check Code Style
        run: ./utils/format -n
